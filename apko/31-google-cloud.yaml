include: apko/30-development.yaml

contents:
  packages:
    # Google Cloud local development kit
    - google-cloud-sdk
    - openjdk-24-default-jdk  # Required for Firestore emulator

environment:
  JAVA_HOME: /usr/lib/jvm/default-jvm
