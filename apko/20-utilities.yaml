include: ./apko/10-tool-defaults.yaml

contents:
  packages:
    # Standard utilities / usability
    - bat
    - difftastic
    - direnv
    - fzf
    - gh
    - git
    - hivemind
    - just
    # Too slow to run in GitHub Actions
    # - jj
    - jq
    - neovim
    - ripgrep
    - starship
    - zoxide

annotations:
  dev.husin.devbox.layer: "20-utilities"
